package ArrayCases;
import Assignment1.Arrays.ArrayInsertion;
import org.junit.Assert;
import org.junit.Test;

public class ArrayInsertionTest {
    ArrayInsertion Array;

    @Test
    public void arrayInsertionTest() {
        int[] array = new int[10];
        /*Array= new ArrayInsertion();
        Array.display(array);
        Array.insert(array, 1, 0);
        Assert.assertEquals(1,array[0]);
        Array.insert(array, 0, 1);
        Assert.assertEquals(0,array[1]);
        Array.insert(array, 5, 2);
        Assert.assertEquals(5,array[2]);
        Array.insert(array, 12, 3);
        Assert.assertEquals(12,array[3]);
        Array.insert(array, 2, 4);
        Assert.assertEquals(2,array[4]);
        Array.insert(array, 90, 5);
        Assert.assertEquals(90,array[5]);

        Array.getArray(array);*/


    }
}
